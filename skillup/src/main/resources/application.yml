spring:
  application:
    name: skillup
  
  # Configuração do Banco de Dados
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/ORCLCDB
    username: skillup
    password: skillup123
    driver-class-name: oracle.jdbc.OracleDriver
  
  # Configuração JPA
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.Oracle12cDialect
  
  # Configuração do Flyway
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  
  # Configuração de Cache
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterWrite=300s
  
  # Configuração do RabbitMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  # Internacionalização
  messages:
    basename: i18n/mensagens
    encoding: UTF-8
    cache-duration: 3600

  # Ativação de perfis por ambiente
  profiles:
    active: dev

# Configuração do Servidor
server:
  port: 8080
  servlet:
    context-path: /api

# Configuração de Logs
logging:
  level:
    root: INFO
    org.springframework: INFO
    com.fiap.skillup: DEBUG

# Propriedades Personalizadas
aplicacao:
  seguranca:
    jwt:
      segredo: ${JWT_SECRET:chaveSecretaSkillup}
      expiracao: 86400000 # 24 horas em milissegundos
  
  # Integração com IA
  ia:
    chave-api: ${CHAVE_IA_API:sua-chave-api-ia}
    endpoint: https://api.openai.com/v1/chat/completions
    modelo: gpt-4

 

---
# Perfil de Desenvolvimento
spring:
  config:
    activate:
      on-profile: dev
  
  datasource:
    url: jdbc:h2:mem:skillup;DB_CLOSE_DELAY=-1;MODE=Oracle
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  
  h2:
    console:
      enabled: true
      path: /h2-console

  flyway:
    enabled: false

---
# Perfil de Produção
spring:
  config:
    activate:
      on-profile: prod
  
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  
  cache:
    type: redis
    redis:
      time-to-live: 3600000

  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:usuario}
    password: ${RABBITMQ_PASSWORD:senha}
